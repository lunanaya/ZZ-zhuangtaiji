# 测试指南

## v0.16.80 剧情扣子简化

- `node dev/smoke-state-logic.mjs`：自然语言扣子仅检查已写条件，未展开/无法自动判断/明确受阻分别显示；缺省字段不制造警告，空条件和冲突条件不判为满足，条件满足不改写事件状态。验证真实注入使用单一状态、受阻原因保留、界面核验默认折叠、原始条件和可能影响保留。
- 回归 `smoke-memory-view.mjs`、`smoke-plain-memory.mjs`、`smoke-flowing-world.mjs`；本地或模拟 API，不调用真实模型。

## v0.16.79 持续演化与去重

- `node dev/smoke-flowing-world.mjs`：不同人物并行行动、非空栏新增候选、多事项独立结束、空栏补全校验、因果减弱而未结束、推测记录退出且不升级为确认历史、80轮稳定性、主动查询/到期提醒交错、改写继承、改期再提醒、序列化、标签去重和相关召回。
- `node dev/smoke-plain-memory.mjs`：实际Engine、HTTP/SSE封装、解析与保存路径，验证正常一次调用可向非空栏新增活动；首轮生成回执在正文结算改写后仍正确保留。保留全栏目、初始化两次、失败、截断与回滚用例。
- 相关回归：`smoke-state-logic`、`smoke-objective-framing`、`smoke-knowledge-boundaries`、`smoke-injection`、`smoke-memory-delivery`、`smoke-worldbook-merged-read`、`smoke-worldbook-semantic`、`smoke-memory-view`、`smoke-turn-read-rollback`。
- 上述模型响应均为模拟，不消耗真实API；测试验证机制及提示约束，不证明任意真实模型的每次推演都符合现实规律。旧结构化模式的按轮降温测试不代表短句模式会按轮数删除事实。

## v0.16.78 两步合并读取

- `node dev/smoke-worldbook-merged-read.mjs`：通过真实 API 封装、SSE 与句子解析器模拟两步初始化；两次均带完整长世界书、角色卡和正文，第二次携带第一步状态。检查归栏后原文备份与原生去重、补充栏允许为空、截断仍不超过两次调用；解析实际设置HTML验证独立页/按钮移除、四个注入位置移入“注入模块”。
- 继续回归 `smoke-plain-memory.mjs`、`smoke-worldbook-memory.mjs`、`smoke-reading-popup.mjs`、`smoke-clear-read.mjs`、`smoke-objective-framing.mjs`、`smoke-worldbook-entries.mjs`。旧版的“所有栏目必填”断言改为“除世界书补充外”，其余更新与失败检查保留。
- 测试使用模拟模型响应，不消耗真实 API，也不声称真实模型已逐条核对实际世界书。

## v0.16.77 世界书直接归栏

- `node dev/smoke-worldbook-semantic.mjs`：所选长书完整进入一次请求，沿用句子JSONL更新、合并和跨栏移动，保留动态状态；成功后不再回传原文；补充使用角色定义/作者注释四种原生位置，缺少作者注释时落到角色定义之后；失败、截断、并发状态变化及跨聊天隔离。测试使用模拟API，不消耗真实额度。
- `smoke-worldbook-memory.mjs` 保留未读取资料的恢复、原文备份、聊天隔离和清空行为；界面断言更新为折叠原文。
- `smoke-worldbook-independent.mjs` 是旧协议测试，依赖已不存在的 `referenceText` / `fallbackCount`；用本次修改前备份复测也失败，与本次直接归栏无关。

## v0.16.76 全栏目客观记录

- `node dev/smoke-objective-framing.mjs`：四种读取任务共用客观记录约束；普通注入及手动结论覆盖都保留防止加码的固定指导；10次相同状态回传没有更强或重复指令，实际出行限制、跟随行为、愿望归属及世界书原文不被机械改写。
- `smoke-plain-memory.mjs`通过HTTP/SSE模拟验证真实各阶段请求含全栏目规则及旧错误解释复核要求，原有API次数、完整性和原文回传保持。`smoke-knowledge-boundaries.mjs`、`smoke-worldbook-memory.mjs`回归通过。
- 未调用真实付费模型，未改写真实聊天旧档。具体旧记录仍由下一次读取/整理根据原始资料修正；不把提示约束测试声称为真实人物描写已逐条审核。

## v0.16.75 不知情边界

- `node dev/smoke-knowledge-boundaries.mjs`：不知情/部分知情/误解在知情者前显示，完整内容不被UI修改；连续12轮无关键词且已发送仍回传完整边界，“没有人知道……”不丢失；没有新证据不自动消失，修改甲的认知不影响乙和其他秘密，明确关闭模块仍有效。
- 同一测试检查实际状态请求包含不知情重点、获知渠道和防止虚构全员不知情的规则。`smoke-plain-memory.mjs`通过模拟HTTP/SSE验证各读取阶段共用该规则，初始化2次、更新/整理1次和全栏目必填保持。
- `smoke-memory-view.mjs`、`smoke-worldbook-memory.mjs`回归通过。角色是否确已获知仍需AI依据来源判断；本地回传和显示测试不等于对真实故事逐条复核。本轮没有调用真实付费API或修改真实聊天。

## v0.16.74 世界书规则简写

运行 `node dev/smoke-worldbook-compression.mjs` 验证可逆简写与拒绝新增/遗漏/改义；`node dev/smoke-worldbook-memory.mjs` 验证实际注册提示和原文对照；`node dev/smoke-plain-memory.mjs` 验证既有全栏目读取。说明与样例数据见 [规则简写验证](docs/worldbook-compression-20260910.md)。

## v0.16.73 世界书承接与 KEEP 修复

运行 `node dev/smoke-worldbook-memory.mjs`、`node dev/smoke-worldbook-entries.mjs`、`node dev/smoke-plain-memory.mjs`，以及既有状态逻辑、显示、清空确认测试。详细覆盖与旧版独立编译测试的不兼容项见 [承接验证记录](docs/worldbook-takeover-20260910.md)。这些测试使用隔离数据和模拟提供商，不调用真实付费 API。

## v0.16.72 清空读取确认窗口

- `node dev/smoke-clear-read.mjs`：实际 UI 确认处理配合隔离 DOM/存储替身，验证打开不清空、取消/Esc、焦点、重复点击、切换聊天、读取并发、保存错误及重试；禁止调用原生 confirm。
- `node dev/smoke-reading-popup.mjs` 与 `node dev/smoke-plain-memory.mjs`：确认读取界面、全栏目必填、原始来源和回传逻辑没有回归。
- 在 `dev/serve-plain-preview.mjs` 提供的独立虚构预览中，实际点击验证弹窗可见、取消保留状态、确认后读取位置重置且“读取当前聊天”恢复可用，控制台无错误。未清空真实聊天、未调用真实模型。
- 真实页面观察到旧按钮未禁用且脚本为 v0.16.71；没有确认原生弹窗被抑制的直接证据。真实聊天的诊断点击被自动审批拦截，随后仅在隔离预览验证修复。

## v0.16.71 全栏目必填修复

- `node dev/smoke-plain-memory.mjs`：模拟HTTP/SSE验证提示词无允许留空指令；初始化模型漏栏即未完成；正文更新带原世界书和上下文；补读原初始化缺项后恢复完整标记，无需清空；依旧2/1/1次模型调用。
- 同一测试验证整理不能删空栏目，逐轮删除最后一项不能成功，只有已完成事项且被生命周期清理后变空也不能成功；未完成不推进读取回执，后续一次更新可补全。
- `node dev/smoke-state-logic.mjs` 和 `node dev/smoke-memory-view.mjs`：确认既有时钟、条件、清理、无截断回传和显示仍可用。
- v0.16.70中允许空栏的预期已撤销。代码只检查模型真实返回的有效记录，不生成假占位来通过完成校验。本轮未调用真实付费模型。

## v0.16.70 时钟、校验与生命周期

- `node dev/smoke-state-logic.mjs`：年号/公历、中文相对日期、首次锚定、重读/措辞更新不顺延、旧相对日期不猜锚点、日期精度、超期不自动完成；权限主体、否定、解除例外；资源数值与AND/OR；触发四类前提；已知有向路线和耗时。
- 同一测试覆盖临时权限到期、日期当天仍有效、规则/秘密/承诺保留、结束结果首次回传、后续静默、主动召回、完整新快照替换、部分信息不误删、25次无变化保存不增加历史、12条长人物记录不受原预算截断、序列化、实际Storage回滚和聊天隔离。全部本地计算，0次API。
- `node dev/smoke-plain-memory.mjs`：模拟HTTP/SSE走实际读取/保存/回传入口，验证初始化最多2次、正文更新1次、截断不追加请求、晚到结果不覆盖新状态、整理与提醒回执。v0.16.71已撤销允许空栏，完整性看end、合法修改和清理后的全栏目覆盖。
- `node dev/smoke-memory-view.mjs`：界面保留原文与本地核验，HTML转义、旧格式兼容、显示不修改状态。
- 兼容回归：`smoke-memory-delivery.mjs`、`smoke-turn-read-rollback.mjs`、`smoke-chat-isolation.mjs`、`smoke-injection.mjs`、`smoke-api.mjs`。
- 当前句子模式不采用下文旧结构版的“预算截短”预期。真实模型的事实抽取和推演质量需另行实测，本轮未调用付费模型。

## v0.16.68 初始化保护与完成诊断

- `node dev/smoke-plain-memory.mjs`：已有内容时 initialize/readFullChat 均拒绝且零调用；清空后开放；开始标记持久化，第二步失败后也不能叠加初始化。
- 分别模拟有效 end + 错误 before、缺少 end，验证提示区分原因并保存诊断，不自动发出第三次 API。
- 核对真实页面已有状态时按钮为灰色，未完成旧提示说明请求已结束；不为了测试清空用户状态或重复调用付费 API。

## v0.16.67 一次 AI 整理

- `node dev/smoke-plain-memory.mjs`：一次请求完成语义合并、跨栏归位、地图和关系格式整理；保留锁定栏目、推演结论、读取位置和轮次；无变化不产生版本；截断、错误 before 或锁定修改均不写入、不重试；快照可恢复。
- `node dev/smoke-api.mjs`：阻止重叠操作共用调用预算；完整读取 2 次上限保持有效。
- 真实页面核对“智能整理 · 1 次 API”与实际改动栏目；对外 API 实测须取得对应授权。模拟测试不代表真实模型整理效果已验证。

## v0.16.66 简短记忆与分类展示

运行 `node dev/smoke-memory-view.mjs` 和 `node dev/smoke-plain-memory.mjs`。覆盖全部记忆栏目、实际 UI 入口、地图包含层级与同名歧义、势力主体、人物聚合、位置冲突保留、关系方向、知识边界、条件例外、旧句兼容、HTML 转义，以及展示不修改存储与投递状态。初始化仍两次，正文后仍一次，不增加排版 API。

`node dev/serve-plain-preview.mjs` 提供独立演示页：地图树可展开收起，人物位置单独显示，双向关系分别展示，各方势力独立成卡，事务按日程、目标、条件链或时间线显示。默认采用简短文本样例，`?legacy=1` 可检查旧结构迁移后的展示。编辑一条短文本、保存并切回卡片，确认内容完整；预览与真实聊天隔离。

## v0.16.63 句子存储完整流程

运行 `node dev/smoke-plain-memory.mjs`。通过模拟 HTTP/SSE 输出验证真实 API 解析、公开引擎入口、保存和界面渲染：初始化两次；第二次生成 NPC 活动并补齐全栏目；后续一次合并更新；每条事实只存字符串；截断和第二次失败仍能显示完整句子；部分结果重试只保留一个最初回滚点；主动查询和到期提醒正常，未变安排不重复投递。测试不调用真实付费模型。

界面检查可运行 `node dev/serve-plain-preview.mjs`，在独立预览页选择人物栏目，直接编辑为一行一句并保存。预览数据与正式聊天隔离。

## v0.16.62 记忆投递回归

运行 `node dev/smoke-memory-delivery.mjs`。该用例模拟同一事项反复更新、13 次成功结算、到期提醒、失败重试和回滚；日志中一次 `simulated interruption` 是故意注入的失败。还应运行 `smoke-injection.mjs`、`smoke-chat-isolation.mjs`、`smoke-truth-status.mjs`、`smoke-affairs-separation.mjs`、`smoke-turn-read-rollback.mjs` 与 `smoke-stream-completion.mjs`。这些测试均不调用付费模型。

## 1. 只看面板

双击根目录的 `preview.html`。

预览使用模拟的夏家场景，打开后应该看到：

- 顶部显示 `WORLD ENGINE`、状态版本和世界时间。
- 左侧或顶部有11个栏目。
- 主区域是当前栏目对应的可编辑中文状态；保存时会自动转成内部 JSON。
- `API与Prompt` 能打开API和两个Prompt编辑器。
- `上一轮回滚` 中只显示一个“回滚上一轮”操作。
- 关闭大面板后，“魔法棒 → 芝芝状态机系统”和右下角 `WORLD` 按钮都能重新打开。

预览页不会连接 SillyTavern，也不会发出API请求。

## 2. 使用本地 Mock API 测完整闭环

这一步不消耗真实模型额度。

在插件目录打开终端：

```powershell
node .\dev\mock-planner-server.mjs
```

看到以下地址即为成功：

```text
http://127.0.0.1:8787/v1
```

然后在已安装插件的 SillyTavern 中打开 `WORLD → API与Prompt`：

```text
API地址：http://127.0.0.1:8787/v1
模型：mock
API Key：留空
```

点击“测试连接”，再点击“初始化 / 重建”。

## 3. UI冒烟测试

依次检查：

1. 初始化后顶部从“等待初始化”变为 `REV n`。
2. “人物概况”中出现Persona、当前角色；核心人物显示身份、位置、重要处境、持续状态与重要物品，活跃NPC保持轻量信息，普通背景NPC不建立长期卡片。
3. “NPC活动轨迹”按人物只显示一条当前活动；人物关系中不出现亲密度、信任度或紧张度数字，知识卡片分别显示L1/L2/L3重要等级与公开状态。
4. 在“世界状态”的 `lockedPaths` 添加 `world.location.current` 并保存。
5. 正常发送一条消息，等待正文生成。
6. 世界时间应推进5分钟，但被锁定的地点不能变化。
7. “本轮 Planner”应显示 `timeAdvanceMinutes: 5`。
8. “最终注入”应包含 `<WORLD_STATE>`。
9. “时间线”在正文生成后增加一条 `Mock结算`。
10. “上一轮回滚”应只出现一个“回滚上一轮”按钮；后台最多保留最近10轮。
11. “世界事件”只显示节点及直接结果，“世界进程”只显示仍在演变的世界级变化，“因果影响”显示事件留下且仍在生效的后果；三者不应复制同一段进展。另构造一组可能重叠的信息：能立即行动的事项只进入任务，等待条件的节点只进入可触发事件，围绕用户的长期问题只进入线程，脱离用户仍会发展的变化只进入世界进程；人物落点只进入人物概况，移动过程只进入NPC活动轨迹；关系态度与事实知识不得互相复制。
12. 打开“设置→注入模块”，关闭“人物关系”并保存；“最终注入”中不应再出现 `[人物关系]`，但人物关系页面仍应正常显示。
13. 顶部“地图、人物、事务、系统”四个分类按钮应分别展开不同的子列表。
14. “场景地图”应显示当前位置、已知地点和通行路线，手机端地点使用单列布局。
15. “内置提示词”应显示全局总规则，以及按世界、人物、事务、系统分组的全部独立模块提示词。
16. “事务 → 剧情推进”应只显示一个当前版本：当前方向、当前变化、下一阶段仍需和用户决策点；方向更新后覆盖旧值，不新增历史条目。它不得与长期线程或世界进程重复全文。
17. “设置 → 骰子”默认不勾选；未勾选时最终注入不应出现“共享骰池”。勾选并保存后发送一条新消息，“本轮后台判断”应显示一个共享随机种和 1–3 枚共享骰；最终注入应以“共享骰池｜可选随机源”开头，且不得出现剧情强度或剧情方向骰。
18. 同时启用剧情节奏与共享骰池：前者只限制每轮推进幅度，后者只为多个合理结果提供随机倾向；任何一方都不得越过用户决策点，也不得因快速档位制造高强度事件。
19. 把注入字符设为较小数值并勾选全部模块；“最终注入”中所有非空模块仍应保留标题，不得整块消失。被截短的模块应显示“本模块已按预算压缩”。
20. 在某个已勾选模块的“内置提示词”中写入唯一测试句，保存后应能在“最终注入”对应模块中看到该句。
21. 使用无效 API 地址发送消息；正文生成前不得产生状态机 API 请求，正文仍可使用旧状态注入。正文结束后的唯一一次结算失败时应保留旧状态并显示错误，不得自动重试。
22. 直接使用状态机生成一轮；检查 Planner 请求应包含内置的 `Phantasm 世界运作逻辑（最高优先级）`、LOAD/PARSE/ADJUDICATE/ADVANCE/COMMIT、场景生命周期、认知分层和有效变化点规则，正文结尾不应出现 `<INDRS>`、`<abstract>`、`<note>` 或 `GM_STATE`。
23. 完成世界书拆解后，顶部应出现与“世界、人物、事务、系统”并列的“世界书注入”；拆解几条就显示几个条目模块。每项应显示段落覆盖、语义块、事实ID与兼容的四类规则。原始来源与Planner source中原文保持不变；Prompt Inspector 的临时正文请求中，所选原文的原生副本应被统一事实投影替代且只出现一次。
24. 在 Prompt Inspector 中确认状态模块分别注册到深度 0–4，而不是合成一个深度 0 提示；给测试世界书条目设置深度 3，拆解后规则仍应位于深度 3。
25. 使用长聊天初始化并记录 Mock API 次数：程序必须先本地扫描全部楼层并建立有来源的语义年表，然后严格产生两次请求。请求 A 一次性接收全部整理后的资料并完整提取每个栏目；请求 B 不再读取原文，只携带 A 的证据独立推演；不得出现第三次请求，也不得逐楼层调用 API。
26. 使用不支持酒馆 `jsonSchema` 的模型线路测试；结构化请求返回 502 后应显示“正在切换兼容模式”，随后以不带 `jsonSchema` 的请求继续。若两种模式都失败，最终错误必须包含任务名以及结构化、兼容两次失败原因。
27. 打开“设置 → 拆解世界书”，确认页面显示酒馆风格的多选下拉菜单；候选仅包含当前全局启用、当前角色主/附加绑定和角色卡内嵌世界书。仅在 `world_editor_select` 编辑器中打开但未启用的书不得显示。
28. 在下拉菜单选择多本并保存，读取源只能包含所选书的启用条目；取消一本后，该书应立即从页面已选列表、读取源、逐轮路由和注入中移除。
29. 打开 SillyTavern 外部世界书编辑器，选择一本未启用也未绑定的书并点击“芝芝：一键拆解本书”，应提示先启用/绑定且不得改变选择；选择一本当前可用书后操作，`selectedBookNames` 和 `entryKeys` 只能包含该书。设置页下拉菜单多选仍应有效；“分解正文”与“拆解世界书”应为两个独立标签页。
30. 使用小于本地阈值的资料初始化，应直接用一个最终建状态请求完成，不得预先调用资料读取、世界书路由或状态切片 API。
31. 使用超过本地阈值的资料初始化，本地语义年表的 `coveredMessages` 必须等于完整历史楼层数，首中尾楼层均需存在来源记录。模拟请求 B 返回 502、超时或 `Failed to fetch`：旧状态必须完整保留，请求 A 的完整证据缓存保留；再次执行时复用 A，只调用 B，任何情况下单次手动读取不得超过两次 API。
32. 关闭“使用酒馆默认 API”并配置 OpenAI 兼容地址，读取时浏览器只能请求本机 `/api/backends/chat-completions/generate`；请求体的 `reverse_proxy` 应为去掉 `/chat/completions` 的地址。不得从浏览器直接 fetch 外部 Planner 地址，也不得在后端失败后回退直连而造成重复扣费。
33. 准备多个世界书条目并点击“立即拆解”：在安全分类预算内最多出现一个 `WORLDBOOK_COMPILE_ONCE`，失败后不得拆批或重试；超过安全预算时应为0次API，直接建立本地无损缓存。两种情况都必须保留首段、中段、尾段和全部段落覆盖。
34. 在窄屏或手机上分别拖动主面板顶部分类栏、栏目栏、设置标签栏和顶部操作按钮栏；手势从按钮文字上开始也必须能够横向滚动，不能误锁成点击。桌面端将鼠标停在横栏后滚动滚轮，应左右移动；选中屏幕外栏目后应自动滚到可见位置。
35. 开始“读取当前聊天”或“重新读取 / 重建”后，读取按钮应变为“终止读取”；点击后当前请求应取消并显示“读取已终止”，旧状态和回滚记录均不改变。
36. 将“普通轮次读取最近正文条数”设为 `0`，普通轮次应读取全部可见正文；设为正数时只读取末尾对应条数。把任一酒馆消息隐藏后，该楼层不应出现在普通轮次或最新用户消息中，但手动完整校准必须纳入并在来源审计的“隐藏但已纳入”中计数。
37. 重建成功后旧 AI 状态、锁定字段、运行时数据与回滚快照均不应残留；读取固定为 2 次：A 必须一次性完整提取全部栏目，B 只基于 A 独立推演。A 漏栏必须失败并保留旧状态，不得发起第三次补读。
38. 连续两轮不触发任何仍可能成立的候选事件，第二轮应保留原 trigger 稳定ID；实际触发、明确过期或条件已不可能的项不得继续留在候选池。候选允许为0，不得为了数量配额自动换新或补满。
39. 构造超过上限的每个状态栏目并保存：已完成任务、已解决线程/进程、resolved/discarded 因果应被删除；仍重要的 occurred 事件节点可以保留，其余项目及内部 history、evidence、memories 应压缩到保留上限。重复保存不应继续增长发送给 Planner 的完整 state。
40. 打开“系统 → 最终注入”，内容应同时包含当前实际启用的 `<WORLD_STATE>` 与本轮已路由世界书规则；关闭任一注入模块并保存后，该模块的标题和正文都应立即消失。点击小铅笔整体修改并保存，Prompt Inspector 中下一次生成只能出现修改后的完整文本，不得再重复注入原状态或世界书规则；该轮结算后应自动恢复动态生成的最终注入。
41. 顶部应分别显示“场景地图、世界、人物、事务、世界书注入、系统”六个一级栏目。点击“场景地图”时不显示二级栏目栏，内容区只出现一个占满可用宽度的完整地图面板；“世界”中不再出现“场景地图”，“当前任务”仍保留在“事务”中。
42. 从角色 A 切换到角色 B 后打开“设置 → 拆解世界书”，下拉菜单必须重新读取当前候选；角色 A 的旧绑定书不得残留，角色 B 新绑定书首次出现时自动选中。手动取消后保存并重开不得被再次强制选中。
43. 连续执行无实质变化、同一任务推进、任务完成三种结算：无变化时 `stateDelta` 应为空且状态条数不变；推进时应更新原稳定 ID 而不是新增；仅返回 `done` 或只核验部分 `completionConditions` 时程序必须恢复为active，只有 `completedConditions` 逐字覆盖全部条件后才允许完成并移除。只有具备长期回顾价值时才新增时间线。再构造过期 L1、超量旧时间线和超量 L3：L1 应自然淘汰，旧时间线应合并为阶段摘要，L3 应全部保留。
44. 初始化包含大量世界书设定、普通饮食细节和少量当前线索的长资料：原始世界书不得被改写或替换，`world.facts` 不得出现整份设定摘要或“吃了什么”；状态条目必须同时具有 L1/L2/L3 与 HOT/WARM/COLD。连续空结算后 HOT 应降为 WARM/COLD，L1+COLD 应删除，L3+COLD 应保留在面板但不出现在“最终注入”；正文重新提及时原稳定 ID 应升回 HOT。没有合格信息的模块应为空，不能由 `moduleInjections` 自由文本强行填充。
45. 顶部“整理状态”中的智能整理必须只产生 1 次 API；只传当前记忆、输出差量，不改写世界书或推进读取位置；必须显示实际改动栏目。有改动时 REV 增加且快照可恢复；无改动时明确提示无需调整；不完整响应、无效替换和锁定修改不能部分保存或额外重试。
46. 打开“设置 → 剧情节奏”：默认应为关闭，最终注入中不得出现 `[剧情节奏]`。依次选择极慢、慢速、中速、快速，最终注入只能增加对应的最大推进幅度，不得生成具体剧情计划；每档都必须包含“速度不等于事件强度”和“不得越过用户决策点”。分别开关自动切场景、自动时间跳跃，确认禁止/允许规则独立变化；重新关闭后整个模块立即消失。
47. 顶部操作栏应依次显示“读取当前聊天｜清空读取｜整理状态｜设置｜回滚上一轮”。点击“清空读取”并确认后，当前状态、剧情推进、读取证据缓存与全部回滚版本都应清空，状态恢复“等待初始化”；聊天消息、角色卡、Persona和原始世界书不受影响。再次点击“读取当前聊天”应从零建立新状态。
48. 检查卡片交互：当前任务、可触发事件、长期线程、非玩家人物概况和正在发生的世界事件应提供“关注、介入、询问/调查”；NPC活动、人物关系、世界进程、因果影响、时间线及user已确认知识应只提供受限查询型按钮；世界状态、剧情推进、玩家自身人物卡和user未知知识不得出现发送按钮。点击NPC活动的调查后，发送文本只能表达“尝试了解某人去向”，不能包含后台地点或活动原文；点击未知知识即使伪造DOM事件也必须被拒绝。关注可将对应记忆改为HOT，但介入和调查不得直接修改事实或保证结果。
49. 用“2026年12月10日＋中国北京”初始化，季节应为“冬季”且真实性为 `derived`；把地点改成无法判断半球的虚构地点后，季节应为“未明确/unknown”，不得只凭厚衣服或月份写死。
50. 原文完全没有天气时，天气应初始化为温和的“多云/system_generated”，并显示生成依据。下一轮尝试让系统天气从“小雨”无依据跳成“暴雪”，应保持“小雨”；正文明确出现异常天气并附 `sourceRefs` 时才允许跳变。
51. user与当前角色没有关系证据时，关系页应同时显示两个方向的 `not_established` 覆盖记录；有单向关系时只为缺失方向保留覆盖记录。覆盖记录不得显示“介入/调查”按钮，也不得注入成已确立关系。
52. 建立 `suspected` 关系、伤势或情绪并附依据，最终注入必须出现“疑似，不得写成事实”；不附新来源把它更新成 `confirmed` 时应仍保持推测，附明确正文楼层后才允许确认。
53. 为身份、秘密、任务、权限或命名地点返回 `system_generated`，保存后应被降为 `unknown`；为这类 `suspected/assumed/system_generated` 条目设置L3时应自动降至L2。带来源的 `confirmed` L3仍须长期保护。
54. 运行 `node dev/smoke-truth-status.mjs`，应输出 `Truth status, constrained inference, and relationship coverage smoke tests passed`。
55. 修改一条已缓存世界书后再次拆解，只应失效该条缓存；其他条目的编译时间、事实ID与语义块不得变化。用205条地点条目验证：每轮路由可只取相关候选，但静态目录和“全部地图”必须仍包含第201条之后的地点。
56. 建立同一事实同时出现在世界书、人物和任务投影的测试数据；最终注入只能由owner模块输出一次。关闭owner模块后该事实才可回退到世界书；依赖事实必须一并加入，规则正文、条件和例外不得被预算切开。
57. “系统 → 最终注入”显示的各深度文本必须与下一次 Prompt Inspector 中的实际扩展提示逐字一致，不得存在第二份独立世界书提示。关闭模块后，预览和实际请求应同时消失。
58. 地图页切换“全部地图 / 玩家已知地图”：前者显示全部世界书静态节点和动态覆盖，后者只显示玩家已知节点；同名同父级静态/动态地点只能显示一次。路线应显示开放状态、预计耗时、危险和权限规则，地图仅在查看/前往/寻人/任务地点相关时注入最小切片。
59. 构造包含亲情、爱、伤害和政治敌对并存的关系，卡片应同时显示双方视角、核心矛盾、依恋、伤害、边界、和解条件与不同场景表现；普通亲密、保护、坦白或争吵不得自动清空伤害、完成和解或升级正式关系。
60. 令一名离屏NPC距离上次后台更新时间达到60分钟：正文后唯一一次结算的 `npcSchedule` 应标为background，且只能沿既存动机/目标/日程/活动推进；返回实际 `npcUpdates` 后程序时钟更新。未到期的carry人物不得新增活动，允许全体无变化。
61. 在酒馆中禁用一个世界书原条目，插件条目列表仍应允许单独勾选它进行预编译，并标明“原条目已禁用”；它可出现在审计缓存中，但不得进入运行时路由。重新启用原条目不应自动改变“纳入编译”的勾选状态，反之亦然。

Mock服务器终端应依次看到：

```text
[Mock Planner] INITIALIZE_WORLD -> 200（小资料初始化仅此一次）
[Mock Planner] POST_GENERATION_RECONCILE -> 200（每轮正文仅此一次状态机调用）
```

## 4. 验证“计划不等于事实”

1. 在Mock返回的 Planner 区域看到各人物的 `npcUpdates`，离场且未到后台更新时间的人物应为 `carry`。
2. 让正文模型生成一段没有明确完成新事件的普通对话。
3. 检查时间线：只应增加基于实际助手正文的 `Mock结算`，不会把 Planner 的 `npcUpdates` 自动写成已完成事件。

真实模型测试时，可在 Planner Prompt 临时加入一个明显但不合理的计划，再确认正文未写出时它不会出现在结算状态。测试后记得恢复Prompt。

## 5. 换成真实 Planner API

确认Mock闭环正常后，再改为真实 OpenAI兼容接口。推荐顺序：

1. 点击“测试连接”。
2. 新建一个测试聊天，不要直接使用重要长篇聊天。
3. 初始化后先人工检查人物知识边界、时间和位置。
4. 连续测试3轮普通对话。
5. 测试一次重新生成或滑动回复，确认不会重复结算。
6. 测试切换聊天，确认不同聊天状态隔离。
7. 最后再用于正式聊天。

## 6. 常见问题

- **测试连接失败**：检查地址是否包含 `/v1` 或完整 `/chat/completions`，并查看浏览器控制台的CORS错误。
- **正文没有注入**：在“最终注入”确认内容存在，再在 SillyTavern Prompt Inspector 中检查最终请求。
- **世界书缺失**：初始化前确认世界书确实已在当前角色、Persona、聊天或全局设置中启用。
- **正文后没有时间线**：等生成彻底结束一秒后重新打开面板，并查看 Planner 错误字段。
- **重复结算**：记录复现操作是普通生成、重新生成还是滑动回复，并保留相应聊天副本用于排查。
- **长资料初始化失败**：本地全量扫描后严格执行两次请求。请求 A 未完整返回18个模块的全部数组和审计即失败；请求 B 失败时保留旧基准快照和请求 A 证据缓存。任何情况都不得追加第三次补读。
